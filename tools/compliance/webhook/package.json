{
  "name": "solomon-billing-webhook",
  "version": "1.0.0",
  "private": true,
  "description": "Stripe subscription-activation webhook that enrolls customers into the Solomon 5-part onboarding email sequence (Days 0, 3, 7, 14, 30).",
  "type": "commonjs",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "npm run build && node --test \"dist/test/*.test.js\""
  },
  "dependencies": {
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.2",
    "@types/supertest": "^6.0.2",
    "supertest": "^7.0.0",
    "typescript": "^5.7.2"
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
